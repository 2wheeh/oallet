{
  "name": "oallet-workspace",
  "private": true,
  "type": "module",
  "license": "MIT",
  "packageManager": "pnpm@10.34.4",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/2wheeh/oallet.git"
  },
  "engines": {
    "node": ">=22.11"
  },
  "scripts": {
    "build": "pnpm -r --if-present build",
    "changeset": "changeset",
    "check": "pnpm lint && pnpm typecheck",
    "format": "biome format --write .",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "release": "pnpm build && changeset publish",
    "test": "pnpm test:unit && pnpm test:e2e",
    "test:pack": "node scripts/test-packed-consumer.mjs",
    "test:build": "pnpm -r --if-present test:build",
    "test:e2e": "pnpm build && pnpm -r --if-present test:e2e",
    "test:package": "pnpm build && pnpm test:pack && pnpm test:build",
    "test:unit": "pnpm -r --if-present test:unit",
    "typecheck": "pnpm -r --if-present typecheck",
    "verify": "pnpm check && pnpm test:unit && pnpm test:e2e && pnpm test:package"
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "@changesets/changelog-github": "catalog:",
    "@changesets/cli": "catalog:"
  }
}
